name: Refresh statistics performance assets

on:
  push:
    paths:
      - '.github/workflows/stats-performance-cache-bust.yml'

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Refresh statistics performance assets
        run: |
          sed -i 's/supabase-config.js?v=20260820safe15/supabase-config.js?v=20260820safe16/g' index.html
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No cache change needed"
          else
            git commit -m "Refresh statistics performance assets"
            git push
          fi
